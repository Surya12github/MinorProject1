package dao;

import entity.Encap;

public interface Myinterface {
    //boolean update(Encap e);  
    boolean create(Encap e);
    int getRemainingLeaves(Encap e);
   // void read();
    }
